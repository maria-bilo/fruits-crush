const Rules = () => {
    return (
        <div className="rules">
            <img src=".src/images/logo."></img>
            <h2 className="rules-descrip">Move the items vertically or horisontally to form the row or the line of three or four squares</h2>
        </div>
    )
}

export default Rules;